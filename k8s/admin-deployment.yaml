apiVersion: apps/v1
kind: Deployment
metadata:
  name: shopnow-admin
  namespace: shopnow
spec:
  replicas: 1
  selector:
    matchLabels:
      app: shopnow-admin
  template:
    metadata:
      labels:
        app: shopnow-admin
    spec:
      containers:
        - name: admin
          image: durga/shopnow-admin:latest
          ports:
            - containerPort: 80
